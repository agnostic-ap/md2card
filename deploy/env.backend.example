# 后端环境变量模板
# 复制此文件为 .env，填入实际值

# 监听端口（默认 3747，由 Nginx 反代，通常不需要改）
PORT=3747

# 监听地址（生产环境保持 127.0.0.1，只允许本机访问）
HOST=127.0.0.1

# 允许跨域的前端域名（必须与实际部署域名一致）
# 本地开发：http://127.0.0.1:5173
# 生产环境：https://your-domain.com
FRONTEND_ORIGIN=https://your-domain.com
